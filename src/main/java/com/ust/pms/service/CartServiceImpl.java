package com.ust.pms.service;

import java.util.List;
import java.util.Optional;

import org.springframework.stereotype.Service;

import com.ust.pms.model.Cart;
import com.ust.pms.repository.CartRepository;

@Service
public class CartServiceImpl{// implements CartService{

	//@Autowired
	/*CartRepository cartRepository;
	
	//Optional<Cart> list=new ArrayList<>();
	@Override
	public Cart getProductsInCart(int id) {
		
		Optional<Cart> cartData = cartRepository.findById(id);
		Cart cart = cartData.get();
		return	 cart;
		 
	}

	@Override
	public String createCart(Cart cart) {
		
		cartRepository.save(cart);
		
		return null;
	}*/

}
